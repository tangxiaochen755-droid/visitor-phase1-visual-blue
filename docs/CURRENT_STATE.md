# 当前状态

更新日期：2026-09-17

## 当前基线

- 主要维护版本：浅蓝设计版。
- 当前本地权威基线：`aab4d11`，包含企业 H5 注册身份选择、员工审核状态演示及后台员工注册审核闭环。
- 远端 `origin/main`：`aab4d11`；本地 `main` 与远端一致。
- GitHub Pages 产品页面已更新至 `aab4d11`，企业 H5、租户后台和超级管理端线上文件均已验证包含本轮逻辑。
- 线上地址：https://tangxiaochen755-droid.github.io/visitor-phase1-visual-blue/
- 黑白版仅作为已确认功能交互基线。
- 本地 `main` 是唯一权威产品基线；远端和线上是按需同步的下游副本，当前产品页面已同步。
- 禁止从远端旧提交、线上旧页面或其他历史版本反向同步覆盖本地基线。
- GitHub Pages 仅用于演示；项目文档、任务记录和决策记录无需在线交付。

## 活动任务

- `TASK-025` 已完成本地首期企业品牌配置：企业名称读取开户信息、登录背景读取实施配置，租户后台只读展示这两项并仅开放 Logo 配置；统一企业 H5 读取三项配置。品牌色和页面结构装修不在首期范围。本任务尚未推送或发布。
- `TASK-024` 已完成统一 H5 注册入口及员工审核状态修正：注册页显式选择访客或员工，字段随身份切换；待审核与审核拒绝员工登录后进入对应状态页，不进入被访人业务页。
- `TASK-023` 已完成员工注册字段与审核状态同步：移除员工工号，所属部门改为员工手工填写，入口统一为“员工注册”，审核时确认企业正式部门，注册状态与账号状态分离。
- `TASK-020` 已完成并纳入本地基线 `a58a3d3`；尚未推送或发布。
- 部署包、部署包测试与主题预览按用户要求仅作本地保留，不进入本次同步基线。

## 已完成

- 访客端、被访人端、租户后台、超级管理后台和扫码设备端原型已形成。
- 代申请改为提交后直接生效，按手机号绑定账号；未注册访客注册后自动补绑。
- 申请记录与访问记录已和后台数据口径映射。
- 后台列表筛选、状态标签、横向滚动、操作列和首页卡片样式已多轮调整。
- 首页插画和四个指标图标使用本地资源。
- 已建立项目持续管理文件和任务编号机制。
- 统一企业前台已完成新一轮本地视觉设计：企业蓝品牌头部、分层卡片、表单控件、筛选状态、记录卡、个人信息、底部导航和弹层样式已统一。
- 个人中心已采用国内 App 常见布局；顶部背景、默认头像和用户资料分层实现，可分别替换资源而不影响结构。
- 已输出 `docs/integration/` 研发联调对接包，覆盖 7 类主体、权限与数据范围、核心流程、状态机、接口建议稿及联调验收清单。
- 已将“多次拜访”产品文案统一为“长期拜访”，并同步前后台原型与研发对接文档。
- 租户后台已增加来访目的增删改、启停和“其他”保留项配置；前台选择“其他”时具体目的必填。
- 被访人申请记录默认显示待审核；当前线上原型的批量操作采用列表多选与二次确认，页面同时支持数量角标和拜访形式筛选，访客与被访人均有代申请醒目标识。
- 统一企业前台已按国内企业小程序/H5 规范完成密度收敛，并在 390×844、430×932 视口验证无横向溢出。
- 企业前台头部已改为标准白色导航栏，页面标题收归导航栏；记录页移除重复标题，日期筛选改为单行入口，审核状态采用文字页签、拜访形式采用紧凑胶囊。
- 申请记录已按国内企业移动端主次筛选模式重构：常驻区仅保留 40px 搜索框与状态 Tab，日期和拜访形式进入不超过 60% 视口的 Bottom Sheet。
- 项目已确立“无特殊说明即按国内产品设计”的默认决策，并新增国内产品 UI 规范和验收检查清单。
- 申请记录与访问记录的搜索框已统一恢复清晰边界和焦点反馈；访问记录同步采用两行主筛选，日期筛选按需弹出。
- 已完成统一前台全页回归：申请表单、申请记录、访问记录、个人信息在访客/被访人角色下保持业务功能与移动端尺寸规范。
- 已完成阶段研发交付简版，集中提供角色权限图、复杂流程图、状态图和前后端责任边界。
- 已确认单次拜访支持出访码结束与预约超时自动结束；长期拜访终止时若访客仍在场则保持来访中并设置独立 `terminationPending=true`，完成出访后转为已终止并清除标记。
- 批量审核已支持批量通过和批量拒绝；批量拒绝统一理由必填并保存至全部成功拒绝记录，申请详情可查看理由。430×932 视口已验证操作按钮、空理由拦截和双动作流程。
- 超级管理端已同步当前企业后台视觉规范，统一导航、指标卡、筛选、表格、状态标签、按钮与弹窗；业务字段和操作流程保持不变。
- TASK-007 已完成：长期拜访支持后台时段配置与前台多时段选择；日期、时段、被访人固定必填；申请拆为审核状态与来访状态；二维码补充授权与码有效期；设备支持扫码设备/扫码中台及延后绑定。
- TASK-008 已完成本地修改：多时段选项改为紧凑勾选块；申请卡片只展示当前主状态；进出记录移入详情并支持按申请编号跳转；上级部门改为层级下拉；租户增加短信发送方式，短信列表移除业务关联。
- TASK-009 已补齐此前视觉版遗漏：超级管理端恢复独立租户管理员账号、人工初始密码、被访人登录方式、独立密码重置、完整开户校验及提醒/到期筛选；统一企业前台恢复密码/验证码登录与修改密码流程。
- TASK-010 已完成本地修改：开户默认使用平台短信渠道且不再选择渠道；开户后可独立切换自有企信通、填写接入凭证并维护 3 条默认短信模板。
- TASK-011 已完成阶段收口：研发交付文件更新到 `ead78c4` 功能基线，新增后续改动编号、设计系统、语义 tokens、Cursor 接手说明和源码交付包。
- TASK-012 已完成交付一致性修正：企业管理员申请处置权限与原型对齐；待终止统一为独立 `terminationPending`；批量审核按条返回结果；单次超时原因统一；Cursor 包仅保留统一企业 H5，并新增切图和样式还原验收说明。
- TASK-013 已完成扫码设备端 1280×800 分辨率适配：桌面端保持双栏，扫码区、结果区、绑定弹层和主操作均在首屏完整显示；已移除高保真界面中的实现机制和异常枚举说明，窄屏兼容与业务逻辑保持不变。
- TASK-014 已输出 273 项本轮可执行的前后台代码测试清单，并新增最新产品逻辑测试基线与 10 项旧流程回归禁用用例；扫码设备端 15 项单独保留暂缓。
- TASK-016 已完成本地高保真原型独立交付包：包含四端正式页面与本地资源，支持离线打开；已清理说明性/原型性展示内容，不包含华纳访客、历史 H5、后端和内部文档。
- TASK-019 已完成：固化“本地与线上一致内容为唯一基线、禁止反向同步”的长期版本规则。
- TASK-020 已完成本地修改：统一企业前台补齐访客与员工自主注册；访客注册后自动补绑同租户同手机号待绑定代申请，员工注册默认进入待审核并由有注册审核权限的后台账号处理，审核前不授予被访人业务权限；后台批量配置、单个添加继续直接建档。员工注册字段与状态的最新口径以 TASK-023 为准。
- TASK-023 已完成本地修改：员工注册不再填写员工工号，所属部门由员工手工填写；后台审核时确认正式部门，注册状态固定为待审核、审核通过、审核拒绝，并与账号状态分离。
- TASK-024 已完成本地修改：统一注册页增加访客/员工身份选择；员工注册关闭时仅保留访客注册；员工待审核、审核拒绝登录状态及拒绝后重新提交闭环已补齐。
- TASK-020 已按 SaaS 标品闭环扩展：访客注册始终开放；超级管理端可按租户开放/关闭员工自注册；租户后台账号可独立配置注册审核权限，有权账号可通过或拒绝员工注册申请。

## 待处理与已知风险

- 每次 UI 修改必须做逐页视觉回归；仅检查 HTML/CSS 规则不足以证明页面正常。
- 线上 GitHub Pages 可能存在短暂缓存，发布后必须验证实际资源和页面。
- 历史修改曾出现黑白版与浅蓝版互相覆盖，后续必须遵守版本职责。
- 历史视觉改版曾因只做尺寸与可点击性验收而遗漏业务字段；后续 UI 修改必须增加“端、模块、字段、操作、状态、校验”业务语义回归，不得仅以视觉截图作为功能完整性证明。
- 后台 Remix Icon 线上 CSS 与 WOFF2 均返回 200 且文件哈希与本地一致，但字体图标仍存在客户端渲染不稳定风险；后续建议迁移内联 SVG。
- “企信通”供应商尚未冻结，不同供应商可能使用 `appId + SecretKey`、`appKey + appSecret` 或其他凭证组合；真实对接前必须以采购供应商官方文档为准。

## 下一步建议

如需对外演示首期企业品牌配置，先复核当前工作区中其他未提交修改的归属，再单独执行提交、推送、发布和线上验证。
