# TASK-002 — 前台页面 UI 视觉设计

- 状态：已上线
- 日期：2026-08-10
- 负责人：Codex

## 基线

- 工作仓库：`visitor-phase1-visual-blue`
- 分支：`main`
- 起始提交：`5814074`
- 最近产品可见提交：`d0f6797`
- 本地、`origin/main` 与 GitHub Pages 首页已核验一致。
- 黑白交互原型保持冻结，仅作为功能与交互对照基线。

## 范围

- 重设计统一企业前台 `frontends/enterprise-h5.html` 的移动端视觉。
- 同时覆盖访客与被访人角色下的申请访问、申请记录、访问记录、个人信息及底部弹层。
- 保留现有字段、状态、筛选、数据映射和操作逻辑。
- 建立一致的颜色、字体、间距、圆角、阴影和交互状态。

## 排除项

- 不修改租户后台、超级管理后台、扫码设备端和原型入口页。
- 不修改黑白交互原型仓库。
- 不新增或调整业务流程、角色权限、字段和状态口径。
- 本任务不包含发布，除非用户后续明确要求。

## 验收标准

- 访客和被访人两个角色的四个主导航页面均正常呈现。
- 表单、记录卡、状态标签、个人信息、底部导航和弹层形成统一视觉体系。
- 交互控件具备清晰的默认、焦点、按压和禁用状态，触控区域不小于 40px。
- 在 390×844、430×932 移动端视口完成逐页截图回归，无横向溢出或底部遮挡。
- 现有关键交互（角色入口、导航、筛选、弹层、二维码、表单校验）通过浏览器检查。

## 结果与证据

- 已在 `frontends/enterprise-h5.html` 增加统一前台视觉系统，未修改业务数据与 JavaScript 逻辑。
- 已完成 HTML 解析、`git diff --check` 与 CSS 静态检查。
- 已使用 Safari 实机渲染并检查访客申请页、申请记录、申请详情弹层、被访人代申请、申请记录、访问记录和个人信息。
- 两个角色的四个底部导航入口均可访问；详情弹层、遮罩、关闭按钮和固定底部导航正常。
- 个人中心已按国内 App 常见结构重构为顶部身份区、账号信息、访客服务、账号与安全及独立退出入口。
- 新增可独立替换的 `assets/profile-header-bg.png` 顶部背景切片，以及可替换的 `assets/default-avatar.png` 默认头像；头像、姓名和身份资料均为前景 DOM 层。
- Safari 已分别验证访客和被访人角色的头像、姓名、角色信息、分组菜单与底部导航。
- 根据视觉回看将个人中心头部由 252px 压缩至 196px，默认头像由 88px 调整为 68px，并缩小工具按钮与身份信息间距；Safari 回归确认首屏信息密度更合理。
- 账号信息卡片向上覆盖头部背景 24px；背景底部增加透明到页面底色的 76px 渐变蒙版，消除图片切片与页面底色之间的硬边界。
- 已将申请记录“异常结束”拆为“已失效”和“审核失败”两个独立筛选，并以 Safari 验证两个筛选各仅返回对应状态记录。
- 已统一申请状态语义色：待处理使用琥珀色、待来访使用蓝色、来访中使用绿色、已结束使用紫色、审核失败使用红色、已失效使用灰蓝色；Safari 验证来访中与已结束已可明显区分。
- 已将“代申请”来源标签收拢到申请标题之后，并为申请表单增加底部安全空间、调整提交栏层级和渐变背景，避免固定按钮遮挡备注区。
- 申请记录与提交栏优化产品提交：`30e7d8d`。
- 已修复申请页入场动画保留 `transform` 导致固定提交栏相对长页面定位的问题；提交区现与底部 Tab 紧邻，不再产生中间空层。
- 已将底部 Tab 调整为国内小程序常见白底细分隔样式，选中态仅使用蓝色图标和文字，不再使用整块圆角底色。
- 已在 430×932 视口完成申请页底部截图回归：提交区与 Tab 间距为 0px，备注框完整可见，提交按钮未遮挡内容；产品提交：`a321e3b`。
- 新增统一前台手机号验证码登录页；登录前隐藏业务外壳与底部 Tab，登录成功后进入申请访问首页。
- 新增可独立替换的 `assets/login-bg.png` 登录背景切图（851×1848），品牌、文案、输入框和按钮均为独立 DOM 层。
- 已验证手机号格式、获取验证码、6 位验证码、协议勾选和登录进入前台流程；演示验证码为 `123456`。
- 已在 430×932、390×844 视口完成登录页截图回归，两种视口均无横向溢出且首屏无需滚动。
- 登录页与背景切图产品提交：`ffaab88`。
- 根据线上回看重新生成登录背景切图，将门禁、盾牌与城市主体缩小并上移至右上区域；同时设置背景纵向偏移，430×932 实测主体完整结束于登录卡片上方约 12px。产品提交：`66e2666`。
- 将登录背景、个人中心背景和默认头像转换为实际展示尺寸的 WebP，页面图片引用从约 1.94 MB 降至约 25 KB；原 PNG 保留为源文件和替换基线。
- 登录页顶部使用 `max(20px, env(safe-area-inset-top))` 兼容 H5 安全区，品牌到欢迎标题的间距由 130px 收紧到 88px，并同步上移背景主体。
- 430×932、390×844 回归均无横向溢出且无需首屏滚动；浏览器确认三张 WebP 实际加载。产品提交：`6ac3595`。
- 产品变更提交：`f9437a0`，已推送至 `origin/main`。
- GitHub Pages 已验证：前台 HTML、默认头像和顶部背景切片均返回 200，线上 SHA-1 与本地文件一致。
- 线上地址：<https://tangxiaochen755-droid.github.io/visitor-phase1-visual-blue/frontends/enterprise-h5.html?role=visitor>
- Playwright Chromium 运行时下载长期停滞在 10%，已终止下载；因此 390×844、430×932 自动化逐页截图尚待补测，不将 Safari 宽屏中的 430px 应用框截图冒充精确视口证据。
