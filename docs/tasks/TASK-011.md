# TASK-011 — 阶段收口与 Cursor 开发交付

- 状态：已完成（本地交付）
- 日期：2026-08-14
- 线上功能基线：`ead78c4`

## 范围

- 复核并同步项目总览、当前状态、决策、变更日志和研发交付文件。
- 将 TASK-007 至 TASK-010 的已确认变化同步到研发首读、角色权限、流程、接口建议和联调验收文件。
- 在交付文件中单列“后续改动/待冻结”，明确区分已确认规则、原型演示和研发建议。
- 输出设计规范、机器可读语义 tokens、Cursor 接手说明和源码交付压缩包。

## 排除项

- 不修改当前页面业务逻辑和视觉效果。
- 不生成真实后端、数据库、OpenAPI 或短信供应商实现。
- 不把尚未确认的权限、重试、二维码离线、企信通凭证字段写成既定需求。

## 验收标准

- 项目基线、交付版本和后续待冻结项表述一致，无旧提交号作为当前基线。
- 研发首读覆盖角色、申请、审核、通行、双状态、设备、短信配置和安全边界。
- JSON 语义 tokens 可被解析，CSS tokens 可直接导入前端工程。
- Cursor 包包含源码、assets、设计规范、tokens、研发交付文件、清单和校验值，不包含 `.git`、`.DS_Store`、缓存或真实凭证。
- 压缩包解压后可通过静态 HTTP 服务打开当前原型入口。

## 验证结果

- 研发首读、权限、流程、接口建议和验收文件已同步 TASK-007 至 TASK-010，并统一标记 `FUTURE-01` 至 `FUTURE-07`。
- Markdown 本地链接检查通过，交付文件未引用不存在的相对路径。
- `semantic-tokens.json` 可被 JSON 解析，包含品牌、文本、表面、边框、状态、字体、间距、圆角、尺寸和阴影语义。
- 本任务未修改 `frontends/`、`assets/`、`index.html`、`tenant-admin.html`、`super-admin.html`，当前页面功能与线上基线保持一致。
- Cursor 压缩包使用 Git 已纳管文件生成；包内不含 `.git`、`.DS_Store`、缓存和真实凭证，外部 SHA-256 文件用于完整性校验。
