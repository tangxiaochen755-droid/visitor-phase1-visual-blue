# TASK-005 — 阶段收口与研发交付简版

- 状态：已完成（已发布）
- 日期：2026-08-12
- 负责人：Codex

## 范围

- 校准项目、当前状态、变更日志、活动任务和研发联调文档中的当前线上基线。
- 输出一份前后端首读的轻量交付文档，重点呈现角色权限图、复杂业务流程图、状态流转和联调边界。
- 统一研发文档中的“长期拜访”产品文案。

## 排除项

- 不替代正式 OpenAPI/Apifox 契约、数据库设计和测试用例。
- 不新增未确认的业务角色、状态或审批规则。
- 本任务不修改产品代码。

## 验收标准

- 项目文档准确记录任务开始时线上提交 `1537967`，发布完成后更新为 `ae3430a`。
- `docs/研发交付简版.md` 可作为前端、后端首次对接的首读文件。
- 权限、申请、代申请、批量审核、扫码通行和状态流转均有简洁图示。
- 详细规则可从简版文档跳转到现有 `docs/integration/` 文件。

## 结果与证据

- 已更新项目基线、活动任务、变更日志和联调入口。
- 已输出前后端研发交付简版，并统一旧文档中的长期拜访文案。
- 已完成边界复核：拆分长期申请与访问批次状态，标注未冻结权限，并区分已确认规则、待确认项和研发建议。
- 已补充单次拜访的出访码/超时自动结束双路径，以及长期拜访在场时“待终止→出访→已终止”的延迟生效路径。
- 已将批量审核更新为批量通过/拒绝双动作；批量拒绝统一理由必填，并由服务端逐条处理和返回结果。
- 已同步前台原型代码并完成 430×932 回归：双动作按钮触控高度 44px、页面无横向溢出、空理由拦截、统一理由保存和批量通过流程均正常。
- 已提交并发布 `ae3430a`；远端 `main` 与 GitHub Pages 均已验证加载新版批量拒绝代码。
