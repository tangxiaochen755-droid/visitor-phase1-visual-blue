# TASK-022 — 收口本地权威基线

## 范围

- 将已完成的 `TASK-020` 产品改动、项目文档和 `TASK-021` 开发环境验收证据纳入版本控制。
- 明确本地 `main` 为唯一权威产品基线，远端仓库和 GitHub Pages 作为按需同步的下游副本。
- 核对本地页面脚本语法、资源引用、Git 差异与版本状态。

## 排除项

- 不推送远端。
- 不发布 GitHub Pages。
- 不重做 `TASK-021` 被开发环境账号和接口阻塞的项目。
- 不生成新的高保真独立交付压缩包。

## 验收标准

- `TASK-020` 源码、文档及 `TASK-021` 验收证据均已提交到本地 `main`。
- 项目文件明确记录本地、远端和线上三者的真实状态与权威关系。
- 三个本轮变更页面的内联 JavaScript 语法检查通过，本地资源引用无缺失，`git diff --check` 通过。
- 工作树干净；未执行推送或发布。

## 状态

- 已完成。

## 验收证据

- 产品与验收归档提交：`a58a3d3`（`完成企业注册流程并归档功能验收`）。
- `frontends/enterprise-h5.html`、`super-admin.html`、`tenant-admin.html` 内联 JavaScript 经 Node.js `--check` 检查通过。
- 三个页面的本地 `src` / `href` 资源引用检查通过。
- `git diff --check` 通过。
- 整理前远端为 `origin/main` `082ad09`，线上产品页面对应 `82b6814`；本轮未推送、未发布。
