# TASK-012 — 交付一致性修正与单一前台入口

- 状态：已完成（本地交付）
- 日期：2026-08-14
- 功能基线：线上 `ead78c4`

## 范围

- 修正交付审计发现的 3 个 P0 与 4 个 P1 冲突。
- 统一角色权限、核心流程、状态、接口建议和联调清单口径。
- 将长期拜访待终止建模为独立 `terminationPending` 标记，不扩展来访状态枚举。
- 统一企业 H5 的终止及批量审核原型行为。
- 重新生成 Cursor 交付包，前台只包含最新 `frontends/enterprise-h5.html`，不包含旧 `visitor-h5.html`、`visitee-h5.html`。

## 排除项

- 不删除仓库中用于历史追溯的旧前台文件，不将其作为交付实现基线。
- 不新增未确认的部门负责人、场所管理员申请处置权限。
- 不实现真实后端、数据库、OpenAPI 或短信供应商连接。
- 不发布线上页面。

## 验收标准

- 企业管理员已在线上原型体现的申请审核、代申请、撤销和终止能力在权限文档中明确；其他后台角色的未冻结权限继续标记待确认。
- `visitStatus` 仅保留五个既定枚举；待终止统一通过布尔字段 `terminationPending` 表达。
- 单次超时结束原因统一为 `TIMEOUT_AUTO_END`。
- 批量审核原型逐条重新校验并展示成功/失败结果，文档明确部分成功语义。
- 联调清单不再要求拜访形式筛选项显示数量。
- 当前基线、超级管理端说明和交付入口清单一致。
- 新交付包不包含旧前台入口，包含完整校验文件并可本地运行。

## 验证结果

- 企业管理员申请处置权限已与租户后台原型对齐；部门负责人、场所管理员继续保留 `FUTURE-02`。
- `visitStatus` 保持五个枚举；统一企业 H5、流程、状态、接口和验收文件均使用独立 `terminationPending` 标记。
- 单次自动结束原因已统一为 `TIMEOUT_AUTO_END`。
- 批量审核原型改为按所选 ID 逐条重新校验，并展示逐条成功/失败结果。
- Cursor 首读、交付清单和样式验收文件已明确唯一企业前台入口；旧访客/被访人入口不进入压缩包。
- 切图、字体、语义 tokens 和样式还原基准已纳入交付；本地资源引用存在性检查通过。
- 新交付包已排除 `.git`、`.DS_Store`、缓存、旧前台入口和真实凭证；包内容及本地 HTTP 启动验证通过。
