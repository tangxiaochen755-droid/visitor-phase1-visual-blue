# TASK-008 — 前台记录卡与后台部门选择视觉收敛

- 状态：已完成，待发布
- 日期：2026-08-14
- 基线：`97eb2fc`

## 范围

- 长期拜访多时段选择改为紧凑、清晰的多选块。
- 前台申请卡片由双标签改为单一主状态；详情仍完整展示审核状态与来访状态。
- 卡片移除进出记录，详情增加当日进出记录，并可按申请编号跳转访问记录页。
- 租户后台上级部门改为标准层级下拉选择。
- 超级管理端租户配置增加短信发送方式：平台默认渠道或租户自有企信通。
- 租户后台短信通知删除历史遗留的业务关联字段。

## 排除项

- 不调整审核、来访、扫码、终止等业务流程。
- 不修改双状态接口口径和访问记录数据模型。
- 不定义企信通账号、密钥或供应商接口字段。

## 验收标准

- 390×844、430×932 下多时段选项无文字挤压，触控区不小于 44px。
- 每张申请卡片只显示一个当前主状态。
- 申请详情能显示当日进出记录；“查看全部进出记录”跳转后自动以申请编号搜索。
- 1366×768、1440×900 下部门弹窗上级部门字段对齐且层级可辨认。
- 新增/编辑租户可选择短信发送方式；短信记录列表、筛选与表单不再出现业务关联字段。

## 验证结果

- 390×844、430×932：多时段选项 46px 高、两列单行对齐，无横向溢出。
- 申请卡片每条仅一个主状态，卡片内进出时间轴为 0；详情显示当日进出记录。
- 详情跳转访问记录后搜索词为申请编号 `VS202607310018`，仅展示该申请关联记录。
- 1440×900：部门上级字段为标准下拉框；短信记录无“业务关联”；新增租户可选平台默认渠道或自有企信通。
- 前台、租户后台、超级管理端浏览器控制台均无 JavaScript 错误。
