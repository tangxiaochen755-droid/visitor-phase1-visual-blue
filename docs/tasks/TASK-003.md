# TASK-003 — 一期研发联调对接包

- 状态：已完成（待联合评审）
- 日期：2026-08-10
- 负责人：Codex

## 目标

基于已确认的一期原型和业务规则，输出可供产品、前端、后端、测试共同评审的角色权限、业务流程、状态机、接口契约与联调验收文件。

## 范围

- 超级管理端、租户管理后台、统一企业前台和扫码设备端。
- 角色权限与数据范围。
- 自主申请、代申请、审核、扫码进出、撤销/终止、账号自动绑定。
- 前后端职责边界、接口建议稿、错误码与幂等要求。
- 联调顺序、RACI、测试场景和待确认项。

## 排除项

- 不修改本轮 UI 和业务代码。
- 不将建议接口路径视为后端已确认实现。
- 不扩展一期已排除的门卫账号、名单管理和站内信。

## 验收标准

- 所有角色均有明确权限与数据范围。
- 核心流程和申请状态可由 Mermaid 渲染。
- 前后端能据接口字段、状态枚举、错误码和幂等规则开展评审。
- 测试可据联调清单覆盖主流程、异常流程和越权场景。

## 结果

- 已在 `docs/integration/` 输出 6 份可版本管理的 Markdown 文件。
- 角色、流程、状态与数据口径来自当前一期业务基线；接口路径明确为建议稿。
- 已执行 `git diff --check`，未发现空白符错误。
- 待产品、前端、后端、测试对 7 项未决问题联合评审后冻结契约。
