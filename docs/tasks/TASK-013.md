# TASK-013 — 扫码设备端适配 1280×800

- 状态：已完成并发布
- 日期：2026-09-10
- 功能基线：本地 `174e62e`，线上设备端与本地修改前文件一致

## 范围

- 将 `frontends/guard-terminal.html` 的桌面设备主视口明确适配为 1280×800。
- 在 1280×800 首屏内完整展示设备头部、扫码区、手工输入区和核验结果区。
- 移除高保真界面中的实现机制、异常枚举等交付说明文字。
- 保留现有窄屏兼容规则和全部扫码交互。

## 排除项

- 不修改租户后台、超级管理后台或企业 H5。
- 不修改设备绑定、扫码核验、通行事件等业务逻辑与字段。
- 不发布线上页面，不重建研发交付压缩包。

## 主次任务与空间预算

- P0：扫码识别、查看核验结果、继续扫码。
- P1：摄像头不可用时手工输入二维码。
- P2：查看设备编码、场所、通行点和在线状态。
- 国内同类模式：门禁核验设备采用顶部设备状态、左侧扫码主操作、右侧即时结果的固定横屏双栏结构。
- 1280×800 预算：顶部 64px；内容区 736px；内容区上下内边距共 40px；标题与间距约 57px；面板可用高度约 639px。桌面宽度保持双栏，窄于 851px 时才切换单栏。

## 验收标准

- 1280×800 下页面无横向或纵向滚动条。
- 默认等待扫码、设备绑定弹层、扫码成功结果均完整位于首屏。
- 扫码区和结果面板等高，页面不出现无意义的底部空洞。
- 手工输入、模拟扫码、继续扫码和绑定确认仍可用。
- 设备端业务文案、字段和状态不丢失。

## 验证结果

- 通过浏览器以 1280×800 视口验证，`window.innerWidth × innerHeight` 为 1280×800，文档滚动尺寸为 1280×800，无横向或纵向滚动条。
- 默认态下双栏面板完整可见；扫码区高度 539px，右侧等待区高度 583px，均位于 800px 首屏内。
- 首次设备绑定弹层完整居中显示，弹层边界为 y=234.5–565.5px。
- 模拟扫码后结果区和“继续扫码”按钮完整可见，按钮底边为 651.78px；继续扫码可恢复等待态。
- 空手工输入仍提示“请输入二维码内容”；浏览器控制台无错误或警告。
- 核对设备端文案、字段、按钮和 JavaScript 逻辑，除布局 CSS 外未修改业务内容。
- 根据高保真交付要求移除页头设备机制说明和核验结果下方的后台机制/失败枚举说明；扫码 Toast 收敛为“核验成功”。保留扫码引导、输入占位和空状态等操作所需文案。
- 已将“中保真可保留评审说明、高保真不得展示交付说明”的分层规则同步至 `ui-project` 技能，并以 `DEC-018` 固化为本项目长期决策。
- 设备端以独立单文件提交 `2dcfa4e` 推送至远端 `main`；GitHub Pages 线上文件 SHA-256 为 `483bed9956a16609d8cf592088b775c07dbac447dbbcc8de0fa3fa21e3b9af04`，与本地验收文件一致。
