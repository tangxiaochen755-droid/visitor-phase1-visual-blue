# 访客系统一期标品后台需求变更梳理

## 文档信息

| 项目 | 内容 |
|---|---|
| 工作模式 | 资料整理模式 |
| 输出深度 | standard（用于本轮原型修改与自查） |
| 版本 | 2026-08-05 变更基线 |
| 变更范围 | 超级管理端、租户管理后台、设备端映射 |
| 不变范围 | 蓝色视觉稿不修改；访客端与被访人端页面本轮不重做 |

## 一句话场景描述

将一期后台从“多类现场账号＋站内通知＋名单管理”的旧模型，收敛为“唯一租户后台管理员、层级部门、设备编码绑定、短信触达、单次/多次拜访与逐条通行记录”的一致业务模型。

## 核心角色与边界

| 角色 | 账号与登录 | 管理边界 |
|---|---|---|
| 超级管理员 | 平台账号 | 开设租户、配置租户后台管理员、选择被访人登录方式、重置租户后台管理员密码 |
| 租户后台管理员 | 每个租户唯一账号；开户时人工设置密码 | 管理本租户全部后台数据 |
| 部门负责人 | 租户后台账号 | 管理本部门及权限范围内业务 |
| 场所管理员 | 租户后台账号 | 管理指定场所业务 |
| 员工（被访人） | 手机号＋密码默认启用；开户时可追加验证码登录，开户后不可更改 | 接收并处理本人来访申请；账号状态、修改密码由员工管理统一维护 |
| 访客 | 固定支持手机号＋密码或手机号＋验证码，自行选择 | 提交和查看本人申请 |
| 扫码设备 | 无人员账号；通过唯一设备编码完成设备绑定 | 仅扫码核验来访码/出访码，不提供进出记录和消息页面 |

> “场所门卫账号/角色”整体退出一期范围，相关菜单、字段、筛选项、绑定关系、通知对象和前台映射同步删除。

## 关键业务规则

### 1. 租户开户与管理员

- 新增租户时同步生成唯一的“租户后台管理员账号”，账号不可重复。
- 开户表单由超级管理员手动输入管理员密码；不再使用系统默认密码，也不要求首次登录强制修改。
- 租户详情和编辑页不回显、不展示密码字段。
- 租户列表/详情提供“重置密码”独立操作，重置时输入新密码并二次确认。
- 被访人登录方式仅在开户时配置：默认勾选“手机号＋密码”，可追加勾选“手机号＋验证码”；至少保留密码登录，保存后不可变更。

### 2. 组织与通行范围

- 部门支持任意合理层级的父子结构；新增/编辑部门时选择上级部门，顶级部门选择“无”。
- “部门通行点/部门可通行点”统一更名为“拜访通行点”，关联现有通行点并支持多选。
- 部门列表及表单删除员工数、后台账号数、排序字段。
- 访客申请选择被访人后，按其所属部门继承该部门配置的全部拜访通行点。

### 3. 员工与后台账号

- 删除“员工登录设置”菜单和独立登录记录。
- 员工管理合并展示账号状态，并提供“修改密码”“启用/禁用”操作。
- 登录方式不在员工编辑页配置；超级管理端开户时的租户级配置仅影响被访人端。
- 后台账户角色固定为企业管理员、部门负责人、场所管理员三类；不再存在门卫角色。

### 4. 场所、通行点与设备

- 删除场所门卫模块、门卫账号、门卫身份编码以及所有“绑定门卫”字段。
- 通行点与场所设备直接绑定；设备以唯一设备编码完成现场绑定。
- 设备端仅保留扫码功能；扫码结果写入后台通行记录，设备端不展示进出记录或通知。
- 通行记录必须记录实际通行设备，便于从设备编码追溯。

### 5. 访客与申请

- 访客管理删除普通/白名单/黑名单及名单审计逻辑。
- 访客列表字段固定为：身份编号、手机号、访客姓名、单位、申请次数、最近来访、账号状态、操作。
- 访客仍以唯一身份编号追溯；手机号换绑不改变身份编号。
- 申请记录必须区分单次拜访和多次拜访：
  - 单次拜访：预约日期＋预约时间段；当日来访码、出访码各仅可使用一次，使用后或超出当日失效。
  - 多次拜访：有效开始日期＋有效结束日期＋可访问时间段；期间内可重复完成“来访—出访”，超出有效期自动失效。
- 列表展示公共摘要字段；详情按拜访形式展示差异字段和每次到访记录，避免将单次“来访中”状态错误覆盖整个多次拜访有效期。

### 6. 通行明细

- 原“进出明细记录”调整为逐事件记录：每次来访一条、每次出访一条。
- 字段统一为：记录编号、申请编号、访客、拜访形式、通行类型、通行时间、场所、通行点、通行设备、核验结果、操作。
- 删除是否来访、是否出访、来访时间、出访时间和白名单字段。
- 多次拜访可对应多组来访/出访事件，均关联同一申请编号。

### 7. 短信通知

- “通知管理/通知公告/我的消息”统一替换为短信通知业务。
- 后台菜单命名为“短信通知”，支持短信模板/发送对象/手机号范围/发送状态/发送时间等管理语义。
- 不再出现站内信、App 消息、已读/未读、门卫通知对象。
- 被访人审批提醒、审核结果、到访提醒等通过短信触达；短信发送结果可在后台追溯。

## 核心闭环

1. 超级管理员开户并创建唯一租户后台管理员 → 配置被访人登录方式 → 租户启用。
2. 租户维护层级部门及多选拜访通行点 → 员工归属末级或任意部门 → 申请自动继承部门拜访通行点。
3. 访客提交单次或多次拜访申请 → 被访人审核 → 生成对应有效期二维码 → 设备扫码写入逐条通行记录。
4. 设备编码 → 绑定设备 → 关联场所/通行点 → 通行记录，可完整追溯。
5. 审批、结果和到访提醒均通过短信发送，并保留发送结果。

## 状态与异常

- 租户管理员账号：启用、禁用；账号唯一冲突时禁止保存。
- 员工账号：启用、禁用；禁用后被访人不可登录且不可作为新的被访人选择。
- 设备：启用、禁用（后台配置）＋在线、离线（设备上报）；禁用或离线时扫码不可用。
- 单次申请：待审核、审核失败、待来访、来访中、已结束、已失效。
- 多次申请：待审核、审核失败、生效中、已结束、已失效；单次到访过程在详情的访问批次中独立记录。
- 扫码异常：二维码过期、已使用、非授权通行点、设备禁用、设备离线，均不生成成功通行事件。

## 自查范围

- 全局检索并清除：场所门卫、门卫账号、绑定门卫、门卫角色、员工登录设置、登录记录、名单状态、普通/白名单/黑名单、名单审计、站内通知、已读/未读、设备端进出记录、部门员工数/后台账号数/排序。
- 校验租户开户、租户详情、部门、员工、设备、访客、申请、通行记录、短信通知之间字段和文案一致。
- 蓝色视觉稿目录不改动。

## 待确认但不阻塞本轮原型的事项

- 短信服务商、短信计费与失败重试策略暂不展开，原型仅表现发送与结果追溯。
- 多次拜访每日可进入次数上限未指定，本轮按有效期内不限次数表达。
- 部门最大层级未指定，本轮按树形层级表达，不设固定上限。

