# 访客标品浅蓝设计版项目约束

## 开始任务

- 完整阅读 `docs/PROJECT.md`、`docs/CURRENT_STATE.md` 和其中指定的活动任务。
- 涉及业务、版本边界、数据映射或发布时，阅读 `docs/DECISIONS.md`。
- 检查 Git 状态、最近提交、远端差异和线上基线后再修改。
- 浅蓝版是当前主要维护版本；不得用黑白版文件整体覆盖本仓库。

## 执行任务

- 使用 `TASK-NNN` 记录范围、排除项、验收标准和证据。
- 保持已确认业务逻辑，视觉修改不得造成字段、操作或状态丢失。
- 发布前核对目标仓库和线上地址；线上验证前不得声称已上线。

## 完成任务

- 更新活动任务和 `docs/CURRENT_STATE.md`。
- 仅在形成长期约束时更新 `docs/DECISIONS.md`，仅在用户可见变化时更新 `docs/CHANGELOG.md`。
- 报告本地、远端和线上三者的实际状态。
