# 访客系统一期功能范围与前台映射

## 一期端范围

- 超级管理端：租户开户、唯一租户后台管理员、被访人登录方式、续租与密码重置。
- 租户管理后台：组织、账号、员工、场所、通行点、设备、拜访、访客、申请、通行明细、短信通知。
- 访客 H5：申请拜访、我的申请、手机号换绑；固定支持手机号＋密码或手机号＋验证码登录。
- 被访人 H5：来访申请记录、审核、代访客发起申请、手机号换绑；登录方式由超级管理员在租户开户时确定。
- 扫码设备端：通过唯一设备编码绑定设备，仅提供来访码/出访码扫码核验。

## 超级管理端

| 模块 | 功能范围 | 关键规则 |
|---|---|---|
| 租户管理 | 查询、新增、编辑、查看、续租、导出 | 租户账号及租户后台管理员账号唯一 |
| 租户后台管理员 | 开户时创建唯一管理员账号 | 管理员姓名、手机号、账号和密码均人工录入；详情与编辑不展示密码 |
| 重置密码 | 列表独立操作 | 输入新密码并二次确认，保存后立即生效 |
| 被访人登录方式 | 开户时配置 | 默认手机号＋密码，可追加手机号＋验证码；开户后不可变更，只影响被访人端 |
| 服务期限 | 到期日期、短信提醒、续租 | 过期后限制租户后台登录 |

## 租户管理后台

| 模块 | 功能范围 | 关键规则 |
|---|---|---|
| 场所管理 | 场所增改查、状态、通行点与设备 | 通行点直接绑定唯一设备编码 |
| 部门管理 | 若依式树形表格、展开/收起、新增下级、树形上级部门选择、负责人、拜访通行点 | 拜访通行点与场所通行点关联并支持多选；父部门删除或停用前校验下级部门与关联人员 |
| 后台账户 | 企业管理员、部门负责人、场所管理员 | 三类角色固定；数据范围沿用本租户、本部门或多选指定场所 |
| 员工管理 | 员工资料、身份编码、可作为被访人、修改密码、启用/禁用 | 登录方式不在此配置；不提供登录记录 |
| 场所设备 | 设备编码、设备名称、场所、通行点、设备启用、设备状态 | 设备编码唯一；启用由后台配置，在线状态由设备上报 |
| 拜访设置 | 单次/长期拜访、预约时间段、来访目的、通行范围、被访人代申请、申请字段 | 来访目的支持增删改和启停，“其他”为保留项；代申请提交后直接生效并按手机号匹配访客账号 ID |
| 短信通知 | 模板、内容、对象、手机号、业务关联、时间、发送状态 | 审批、结果、到访等业务消息通过短信发送 |
| 访客管理 | 身份编号、手机号、姓名、单位、申请次数、最近来访、账号状态 | 不设置名单状态；手机号换绑不改变身份编号 |
| 访客申请记录 | 单次/长期申请审核、申请来源标签、账号绑定状态、访客账号 ID、二维码、拒绝原因、访问批次 | 申请自动继承被访人部门的多个拜访通行点；代申请支持撤销和提前终止 |
| 通行明细记录 | 来访、出访逐事件记录 | 每次通行独立一条，记录场所、通行点、通行设备和结果 |

## 前后台映射

| 前台端 | 前台功能 | 后台映射 | 数据与规则 |
|---|---|---|---|
| 访客 H5 | 登录 | 访客管理 | 固定支持手机号＋密码或手机号＋验证码，自行选择 |
| 访客 H5 | 申请拜访 | 拜访设置、员工管理、部门管理、访客申请记录 | 搜索启用员工；继承员工部门的拜访通行点；按单次/长期显示差异字段 |
| 访客 H5 | 我的申请 | 访客申请记录 | 查看审核状态、有效期、二维码、拒绝理由和访问批次 |
| 访客 H5 | 修改手机号 | 访客管理 | 原手机号＋验证码＋新手机号；身份编号保持不变 |
| 被访人 H5 | 登录 | 超级管理端租户开户配置、员工管理 | 手机号＋密码固定启用，验证码登录是否可用由开户配置决定 |
| 被访人 H5 | 来访申请记录与审核 | 访客申请记录、短信通知 | 审核通过生成相应二维码，拒绝必须填写理由，结果短信通知访客 |
| 被访人 H5 | 代访客发起申请 | 拜访设置、员工管理、部门管理、访客申请记录、短信通知 | 被访人提交后单次直接进入待来访、长期直接进入生效中；按手机号绑定已有账号，未注册时等待注册后自动补绑 |
| 被访人 H5 | 修改手机号/密码 | 员工管理 | 身份编码不变；密码可由本人或后台修改 |
| 扫码设备端 | 设备绑定 | 场所设备、通行点 | 输入或扫描唯一设备编码完成绑定，无人员账号登录 |
| 扫码设备端 | 扫码确认 | 访客申请记录、通行明细记录 | 仅核验来访码/出访码；成功后写入一条带设备编码的通行事件 |

## 申请与通行状态

### 单次拜访

待审核 → 审核失败 / 待来访 → 来访中 → 已结束。来访中可通过出访码正常结束；到达预约结束时间仍未出访时由系统自动结束，并区分结束原因但不伪造扫码离开事件。尚未进入且超过预约日期或二维码有效期后为已失效。来访码、出访码当日各限成功使用一次。

### 长期拜访

待审核 → 审核失败 / 生效中；有效期内每次扫码形成独立的来访与出访事件，访问批次结束不改变申请的生效中状态；超过结束日期后为已失效。提前终止时若无人仍在场则立即已终止；若访客仍在场则先待终止并禁止再次进入，完成出访后转为已终止。

## 跨模块一致性规则

- 人员身份编号、设备编码均为唯一且不可变的追溯标识。
- 部门拜访通行点只引用已启用通行点；通行点删除前必须解除设备绑定。
- 设备禁用或离线时不可完成有效扫码核验。
- 员工禁用后不可登录，也不可作为新申请的被访人。
- 被访人代申请提交后直接生成通行权限，不设置访客确认节点。
- 系统优先按本企业内手机号匹配访客账号 ID；未注册手机号的申请标记为“待账号绑定”，访客以后使用同一手机号注册时自动补绑，申请与通行历史保持不变。
- 一期被访人代申请免审并直接生效；租户二次审批不纳入当前一期，如后续扩展需另行确认流程与权限。
- 代申请首次通行前可撤销；长期拜访可提前终止剩余授权，历史通行记录继续保留。
- 短信发送失败保留失败状态，具体重试策略不在一期原型展开。
- 原型二维码为静态展示，不模拟固定时效刷新。
- 来访目的保存配置项 ID 与申请时名称快照；选择“其他”时具体目的必填，历史申请不随配置变更。
- 被访人申请记录默认展示待审核；批量审核支持批量通过和批量拒绝。批量拒绝必须填写一个统一理由并应用于本次勾选的全部记录；服务端逐条校验状态与权限并返回逐条结果。
